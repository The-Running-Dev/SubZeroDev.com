// Content — the two named copy constants (contract's `primarySlogan` and
// `apexFooterQuote`). `Idea.md`'s *Slogans* and *Footer Quotes* sections
// enumerate more than these; per the design's *Copy* section and *A fixed
// slogan, rather than rotation*, exactly one of each is a named constant —
// there is no rotation and no collection.

export const primarySlogan = "Well… Why not?" as const;

export const apexFooterQuote = "Trust us… It'll be fine. Or not." as const;
